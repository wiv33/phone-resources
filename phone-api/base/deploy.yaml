apiVersion: apps/v1
kind: Deployment
metadata:
  name: phoneshin-api-deploy
  labels:
    app: phoneshin-api
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: phoneshin-api
  template:
    metadata:
      name: phoneshin-api
      labels:
        app: phoneshin-api
    spec:
      containers:
        - name: phoneshin-api
          image: 'registry-harbor.phoneshin.com/phoneshin/api:latest'
          imagePullPolicy: Always
          resources:
            requests:
              cpu: "1000m"
              memory: "4Gi"
            limits:
              cpu: "1000m"
              memory: "8Gi"
          env:
            - name: TZ
              value: Asia/Seoul
            - name: DEPLOY_ENV
              value: prod
            # Phase 7.5: fabric8 K8s 클라이언트 빈 활성 — SyncTriggerService 사용
            - name: PHONESHIN_K8S_ENABLED
              value: "true"
          ports:
            - containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 8
            successThreshold: 1
            failureThreshold: 7
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
      restartPolicy: Always
      # Phase 7.5: SA — namespace 내 CronJob/Job 조작 권한 (rbac.yaml 참조)
      serviceAccountName: phoneshin-api-sa
      imagePullSecrets:
        - name: inner-harbor-registry
